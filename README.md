# doubao-asr

Local workspace for Doubao ASR experiments and development tooling.

## Development

Activate the project-local environment before working:

```powershell
. .\enter-dev.ps1
```

This enables the local Python virtual environment, Rust/Cargo toolchain, and portable build tools under `.devtools`.
