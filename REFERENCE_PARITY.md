# 参考功能复刻对照

参考仓库：`https://github.com/xiaohu31/doubao-voice-helper`

本项目不是直接调用豆包桌面版悬浮窗，而是内置豆包 ASR 协议客户端，所以实现方式不同；用户可见功能按参考工具逐项对齐。

| 参考功能 | 参考行为 | 当前状态 | 本项目实现/证据 |
|---|---|---|---|
| 按着说 | 按住右 Ctrl，说话，松开自动插入 | 已覆盖 | `hold_key=右 Ctrl`；`--hold-release-auto-insert-test` 覆盖松手自动插入 |
| 自由说 | 鼠标侧键 1 开始，再按一次结束并插入 | 已覆盖 | `toggle_key=鼠标侧键 1`；结束后保留悬浮结果，可自动插入 |
| 按着说+自动发送 | 左 Ctrl + 左 Win 松手后插入并 Enter | 已覆盖 | `hold_send_key=左 Ctrl + 左 Win`；自动插入报告覆盖 `auto_send=true` |
| 取消键 | 自动发送说话中按取消键取消 | 已覆盖 | 默认 `Esc`，录音中触发取消，不插入不发送 |
| 豆包快捷键 | 配置 Ctrl+D 唤起豆包语音 | 部分等价 | 本项目直接调用 ASR，不依赖豆包桌面版；保留 `doubao_hotkey` 设置用于兼容参考 UI/文档 |
| 剪贴板保护 | 插入后恢复原剪贴板 | 已覆盖 | 文本、`CF_DIB` 图片、`CF_HDROP` 文件列表有安装版烟测 |
| 智能无内容处理 | 无识别内容时快速关闭悬浮窗 | 已覆盖 | 空文本不会插入；按着说自动收起悬浮窗 |
| 防抖/防重复 | 处理中不重复触发 | 已覆盖 | `recording_mode` 和精确热键匹配阻止重复触发 |
| 失焦修复 | 先激活豆包悬浮窗再确认 | 等价覆盖 | 本项目保存录音开始前窗口句柄，插入前回到目标窗口 |
| 灵活按键 | 鼠标、功能键、字母数字、组合键、左右修饰键 | 已覆盖并更保守 | 支持热键录制、左右 Ctrl/Win/Alt/Shift 显示；禁止危险裸字母和保留组合键 |
| 热键冲突提示 | 注册失败或配置冲突提示 | 已覆盖 | 保存和录制时检测重复、保留组合、系统占用组合 |
| 托盘启用/禁用 | 托盘菜单可切换启用状态 | 已覆盖 | 托盘新增“已启用/已禁用语音监听”，禁用后不响应录音热键 |
| 托盘显示当前热键 | 右键菜单列出各模式热键 | 已覆盖 | 托盘菜单灰显展示按着说、自由说、按着说+发送、取消键 |
| 首次运行设置窗口 | 首次运行自动弹设置 | 待补 | 当前默认显示主设置窗；隐藏启动不会强制弹前台 |
| 开机自启动 | 默认开启，可配置 | 部分覆盖 | 本项目默认关闭，用户可开启；更保守，避免安装后擅自自启 |
| 帮助/关于 | 托盘和窗口提供帮助 | 已覆盖 | 内置使用说明和分发包 `HELP.md` |

## 还需要继续追齐

1. 托盘菜单增加“帮助/关于”，更贴近参考工具。
2. 增加可配置托盘通知开关，避免错误提示打扰。
3. 把“首次运行自动弹设置”的判定写成明确逻辑和测试。
4. 给自由说模式增加更明确的状态监控报告：开始、停止、取消、无内容。
5. 用真实目标窗口补人工证据：记事本、浏览器输入框、聊天框。
